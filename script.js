function isIsogram(str){
    str = str.toLowerCase();
    let arr = str.split('');
    let match = []
    
    if (match.length = 0 ) {
      arr.forEach(letter => {
        if (letter == arr[i]) {
          match.push(letter)
        }
      })
    }
   }
   
  isIsogram("Dermatoglyphics")
  isIsogram("aba")
  isIsogram("moOse")